---
 - name: Ansible
   hosts: all
   become: true
   tasks:
     - name: git
       command: sudo apt install git
     - name: bash
       command: git clone https://github.com/Makson8286/pasta
             

